import React from "react";

const VideoList = props => {
  return (
    <div>
      {props.video.length}
      <link rel="video list " href={props.Video} />
    </div>
  );
};
export default VideoList;
